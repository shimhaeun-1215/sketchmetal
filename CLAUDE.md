# 스케치금속건설 홈페이지 - 작업 맥락 (Claude Code용)

이 파일은 이전 대화에서 정한 내용을 이어받기 위한 메모입니다. 코드가 기준이고, 아래는 "왜 이렇게 되어 있는지"입니다.

## 회사 정보 (`assets/js/app.js` 맨 위 `SITE` 에 있음)
- (주)스케치금속건설 / 대표 권대욱 / 사업자등록번호 106-87-15952 / 건설업등록번호 부산진2026-다-02
- 업종: 금속·창호·지붕·건축물조립공사업 (주력: 금속구조물·창호)
- 부산광역시 부산진구 백양대로 312, 2층(개금동) / Tel 051-895-0888 / sysoo222@hanmail.net
- 대표는 동아대학교 미술대학 조소과 출신, 금속 현장 15년 이상. 법인은 2026년에 새로 세웠고, 그 전에는 전문건설업체를 운영. 슬로건은 "미대출신의 감각, 금속기술의 완성"

## 구조 (정적 사이트, 해시 라우팅 `#/...`, DB 없음)
- 홈 / 회사소개(인사말, WHY SKETCH METAL + 비전, 오시는 길) / 시공사례 8탭(전체, 주요 시공처, 6개 분야) / 견적문의
- 시공사례 36건, 사진 132장. 카드를 누르면 사례 상세(`#/case/<id>?from=<탭>`)
- 화면은 `app.js` 의 `page*()` 함수가 그립니다. 사례 데이터는 `TABS`, `CASES`, `CLIENTS`, `FEATURED`, `TOP3`

## 디자인 결정 (사용자가 직접 정하거나 반복해서 요구한 것)
- 색: 네이비 `#0A2A4A`, 차콜 `#22272C`/`#111315`, 골드 `#F0B429`(글자용 짙은 골드 `#8F6210`), 연한 회색 `#F2F5F8`/`#EAEEF2`. 로고의 파랑은 상호명 이미지 안에서만 쓰고 UI 색으로는 쓰지 않음
- 글꼴: 제목·본문·메뉴·버튼을 모두 눈누 기초고딕으로 통일(못 불러오면 Noto Sans KR). 예외는 두 가지뿐 — 첫 화면 슬로건의 나눔손글씨 펜, 한자 장식(匠·美·精·信·築)의 명조(`SKSerif`). 상호명은 글꼴이 아니라 이미지. Tailwind의 `font-bold`는 600으로 재정의되어 있고, 기초고딕은 굵기가 400 하나뿐이라 `font-synthesis:none`(custom.css)으로 가짜 볼드를 막음
- 첫 화면: 네이비 격자 면 + 차콜·골드 대각선 위에 3D 로고(three.js r128 CDN, 로고 윤곽을 압출). 상호명은 간판의 3D 금속 글자를 잘라 만든 이미지이고 "(주)"는 글자 높이에 맞춰 텍스트로 얹음(`.wm-ju`의 `cqw` 값은 **눈누 기초고딕 700 기준**으로 맞춘 것. 글꼴을 바꾸면 상호명 이미지와 높이가 어긋나므로 다시 맞춰야 함)
- 견적 문의하기 버튼: 기본 검정, 올리면 단색 금색 `#F7B923`(`.btn-metal`). 금색 면 위에 놓이는 `.on-gold` 변형만 네이비로 반전
- 홈 주요 시공처(1×3)·시공분야(2×3) 카드: 사진 4:3 통일, 올리면 위로 20px + 금색 선, "자세히 보기" 버튼 없음. 시공사례 목록은 어느 탭이든 한 줄 3개, 사진 4:3 통일
- 상단 메뉴: 회사소개·시공사례에 올리면 검은 가로 띠로 하위 메뉴가 펼쳐짐
- 하단 바: 검정, "라벨 | 값" 형식(대표 | 권대욱 ...). 오시는 길: 지도 → 주소·전화 상자 → 네이버지도(파랑)·카카오맵(노랑) 두 버튼
- 견적문의 페이지는 석경건설(sukkyoung.co.kr) 레이아웃을 참고: 왼쪽 직접 연락처, 오른쪽 폼 카드. 좌우 제목 크기는 같게(`text-s5`), 왼쪽 라벨은 Tel / E-mail / 소재지 / 법인 이고 `.eyebrow` 의 대문자 변환을 `normal-case` 로 끔

## 사용자가 원하는 톤 (지켜 주세요)
- 홈과 오시는 길에는 설명 캡션을 두지 않음. 문구는 짧게, 사실만. AI가 쓴 티가 나는 과장·상투어 금지
- 확인되지 않은 수치·주장을 쓰지 않음. 새 문구를 쓰면 사실 확인이 필요한 부분을 알려 줄 것

## 아직 끝나지 않은 것
- 견적 문의 메일: Web3Forms 연결 완료. 회사 메일 sysoo222@hanmail.net 으로 갑니다. 받는 주소를 바꾸려면 Web3Forms 에서 키를 새로 받아 `SITE.formKey` 를 교체하세요
  - 주의: 문서와 달리 주소 뒤에 키를 붙이는 방식(`/submit/<키>`)은 JSON 요청에서 500 이 납니다. `/submit` 으로 보내고 본문에 `access_key` 를 넣어야 합니다(`sendQuote`)
  - 무료 한도 월 250건. 도메인 제한 기능을 찾지 못했으므로 키가 공개된 상태에서 스팸이 오면 hCaptcha 추가를 검토할 것. 허니팟(`f-web`)은 이미 있음
- 카카오맵: 연결 완료. 실제 카카오 서버로 확인했고 백양대로 312(35.1543/129.0173)에 마커가 찍힘
  - 새 도메인(.co.kr 등)을 붙이면 카카오디벨로퍼스 [앱]>[플랫폼 키]>[JavaScript SDK 도메인]에 그 주소를 추가해야 지도가 뜹니다. 안 하면 `domain mismatched` 오류
- 확인이 필요한 것: 국립과학관 실제 기관명, "15년 이상" 표기, 공공기관명 게시 가능 여부, 시공 사진 속 작업자 얼굴 노출(49번 등), 비전의 "사례를 계속 올려 두겠습니다" 약속 문구, WHY ③ 제목(스케치 원안은 "믿을 수 있는 기업", 현재는 "등록된 건설업 법인")
- 인사말의 "권대욱"은 **실제 자필 서명이 아니라 나눔손글씨 붓(구글 글꼴)로 만든 장식**(`.sign`). 대표님 자필 서명을 스캔해 이미지로 교체하는 편이 맞음. 실제 서명처럼 보이게 손으로 그려 넣지는 말 것
- 사례 상세: 해시태그 칩 → 큰 사진(좌우 원형 화살표, 01 / 06 카운터) → 썸네일 줄(선택 시 금색 테두리) → 시공처·분야·문의 버튼 순서. 갤러리 동작은 `galImg`/`data-gstep`/`data-gi` 가 맡음
- 사례별 해시태그는 `TAGS` 에 있음(사례마다 5개). 사진과 사례명에서 뽑은 것이고 없는 사실은 넣지 않았음
- 배너 사진 칸은 약 4.8:1 이라 4:3 사진은 높이의 28% 만 보임. 그래서 배너에는 가로로 넓게 읽히고 작업자가 없는 사진을 골라야 함(시공사례 `c44_0`, 회사소개 `c34_0`)
- 네이버·카카오 아이콘은 단순하게 다시 그린 것. 개인정보처리방침 페이지는 없음
- 로고 마크(563×754)와 상호명 이미지는 간판 배너에서 잘라 낸 것이라 큰 화면에서 약간 부드러움. 벡터 원본이 있으면 교체하고 3D 윤곽(`LOGO_CONTOUR`)도 다시 뽑아야 함

## 작업할 때 알아 둘 것
- 새 Tailwind 클래스를 쓰면 `npm install` 후 `npm run css` 로 `assets/css/tailwind.css` 를 다시 만들어야 함
- `assets/fonts/` 는 사이트에 쓰인 글자만 담은 부분 글꼴. 글자를 새로 추가하면 구글 글꼴이 대신 채우지만(온라인일 때), 정확히 맞추려면 다시 만들 것: Google Fonts 저장소(github.com/google/fonts, ofl/notosanskr, ofl/notoserifkr)의 가변 TTF를 받아 `fonttools varLib.instancer <ttf> wght=300:700`(명조는 400:700)으로 굵기 범위를 줄인 뒤, `index.html`·`app.js`·`images.js`에 쓰인 글자로 `pyftsubset --flavor=woff2` 실행
- 사진은 webp, 긴 변 1000px 안팎. 사례 추가 절차는 README 참고
- 글꼴(눈누 기초고딕)은 `fonts.css` 의 jsDelivr 주소로 불러옵니다. 라이선스는 수정·재배포까지 허용(유료 판매만 금지)이라 서브셋을 만들어도 되지만 지금은 원본을 그대로 씁니다. 굵기가 400 하나뿐이라 `font-bold`(600) 자리는 브라우저 합성 볼드입니다. 원본: noonnu.cc/font_page/1496
- 파일명에 `con`, `prn`, `aux`, `nul`, `com1`~`com9`, `lpt1`~`lpt9` 를 단독으로 쓰지 말 것. Windows 예약 장치 이름이라 git 이 색인하지 못하고(`unable to index file`) Windows 에서 clone 도 안 됨. 건설업등록증 사진이 원래 `con.webp` 였고 이 때문에 `con-cert.webp` 로 바꿨음(`images.js` 경로만 수정, 키 `IMG.con`·`data-zoom="con"` 은 그대로)
- 로컬 확인은 `python3 -m http.server 8000` (파일을 더블클릭으로 열면 3D 로고 텍스처가 막힐 수 있음)
- 이전 대화에서 만든 claude.ai 미리보기 링크는 별개 복사본이라 이 저장소를 고쳐도 자동으로 바뀌지 않음
