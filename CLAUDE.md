# 스케치금속건설 홈페이지 - 작업 맥락 (Claude Code용)

이 파일은 이전 대화에서 정한 내용을 이어받기 위한 메모입니다. 코드가 기준이고, 아래는 "왜 이렇게 되어 있는지"입니다.

## 회사 정보 (`assets/js/app.js` 맨 위 `SITE` 에 있음)
- (주)스케치금속건설 / 대표 권대욱 / 사업자등록번호 106-87-15952 / 건설업등록번호 부산진2026-다-02
- 업종: 금속·창호·지붕·건축물조립공사업 (주력: 금속구조물·창호)
- 부산광역시 부산진구 백양대로 312, 2층(개금동) / Tel 051-895-0888 / sysoo222@hanmail.net
- 대표는 동아대학교 미술대학 조소과 출신, 금속 현장 15년 이상. 법인은 2026년에 새로 세웠고, 그 전에는 전문건설업체를 운영. 슬로건은 "미대출신의 감각, 금속기술의 완성"

## 주소
- 본 주소 **https://www.sketchmetal.co.kr** (가비아에서 2026-09-22 등록, 2029-09-22 까지)
- `sketchmetal.co.kr` 은 `www` 로 308 넘김. `sketchmetal.vercel.app` 도 계속 살아 있음
- DNS 는 가비아 네임서버(ns.gabia.co.kr)에 레코드 두 줄: `A @ 216.198.79.1`, `CNAME www 8fa4469a8468b02a.vercel-dns-017.com.`
- 도메인을 또 붙이면 카카오디벨로퍼스 [앱]>[플랫폼 키]>[JavaScript SDK 도메인]에 반드시 추가할 것

## 구조 (정적 사이트, 해시 라우팅 `#/...`, DB 없음)
- 홈 / 회사소개(인사말, WHY SKETCH METAL + 비전, 오시는 길) / 시공사례 8탭(전체, 주요 시공처, 6개 분야) / 견적문의
- 시공사례 36건, 사진 134장 + 동영상 2개. 카드를 누르면 사례 상세(`#/case/<id>?from=<탭>`)
- 화면은 `app.js` 의 `page*()` 함수가 그립니다. 사례 데이터는 `TABS`, `CASES`, `CLIENTS`, `FEATURED`, `TOP3`

## 디자인 결정 (사용자가 직접 정하거나 반복해서 요구한 것)
- 색: 네이비 `#0A2A4A`, 차콜 `#22272C`/`#111315`, 골드 `#F0B429`(글자용 짙은 골드 `#8F6210`), 연한 회색 `#F2F5F8`/`#EAEEF2`. 로고의 파랑은 상호명 이미지 안에서만 쓰고 UI 색으로는 쓰지 않음
- 글꼴: 제목·본문·메뉴·버튼을 모두 눈누 기초고딕으로 통일(못 불러오면 Noto Sans KR). 예외는 두 가지뿐 — 첫 화면 슬로건의 나눔손글씨 펜, 한자 장식(匠·美·精·信·築)의 명조(`SKSerif`). 상호명은 글꼴이 아니라 이미지. Tailwind의 `font-bold`는 600으로 재정의되어 있고, 기초고딕은 굵기가 400 하나뿐이라 `font-synthesis:none`(custom.css)으로 가짜 볼드를 막음
- 첫 화면: 네이비 격자 면 + 차콜·골드 대각선 위에 3D 로고(three.js r128 CDN, 로고 윤곽을 압출). 상호명은 간판의 3D 금속 글자를 잘라 만든 이미지이고 "(주)"는 글자 높이에 맞춰 텍스트로 얹음(`.wm-ju`의 `cqw` 값은 **눈누 기초고딕 700 기준**. 맞출 때는 괄호까지 포함한 상자가 아니라 **한글 "주" 글자 높이**를 상호명 이미지의 글자 높이와 비교해야 함. 상호명 이미지는 1700×221 에 위아래 5px 여백이 있어 글자가 이미지 높이의 95.5%. 글꼴을 바꾸면 다시 맞출 것)
- 견적 문의하기 버튼: 기본 검정, 올리면 단색 금색 `#F7B923`(`.btn-metal`). 금색 면 위에 놓이는 `.on-gold` 변형만 네이비로 반전
- 홈 주요 시공처(1×3)·시공분야(2×3) 카드: 사진 4:3 통일, 올리면 위로 20px + 금색 선, "자세히 보기" 버튼 없음. 시공사례 목록은 어느 탭이든 한 줄 3개, 사진 4:3 통일
- 상단 메뉴: 회사소개·시공사례에 올리면 검은 가로 띠로 하위 메뉴가 펼쳐짐. 헤더 높이는 `h-[5.6rem]`(89.6px)이고, 이 값을 바꾸면 홈 첫 화면 높이 `calc(100dvh-5.6rem)`, 시공사례 탭 바 `sticky top-[5.6rem]`, 주요 시공처 앵커 여백 `.grp{scroll-margin-top}` 도 같이 고쳐야 함
- 하단 바: 검정, "라벨 | 값" 형식(대표 | 권대욱 ...). 오시는 길: 지도 → 주소·전화 상자 → 네이버지도(파랑)·카카오맵(노랑) 두 버튼
- 견적문의 페이지는 석경건설(sukkyoung.co.kr) 레이아웃을 참고: 왼쪽 직접 연락처, 오른쪽 폼 카드. 좌우 제목 크기는 같게(`text-s5`), 왼쪽 라벨은 Tel / E-mail / 소재지 / 법인 이고 `.eyebrow` 의 대문자 변환을 `normal-case` 로 끔

## 사용자가 원하는 톤 (지켜 주세요)
- 홈과 오시는 길에는 설명 캡션을 두지 않음. 문구는 짧게, 사실만. AI가 쓴 티가 나는 과장·상투어 금지
- 확인되지 않은 수치·주장을 쓰지 않음. 새 문구를 쓰면 사실 확인이 필요한 부분을 알려 줄 것

## 아직 끝나지 않은 것
- 견적 문의 메일: Web3Forms 연결 완료. 회사 메일 sysoo222@hanmail.net 으로 갑니다. 새 도메인에서도 실제로 메일이 도착하는 것을 사용자가 확인했습니다(2026-09-22). 받는 주소를 바꾸려면 Web3Forms 에서 키를 새로 받아 `SITE.formKey` 를 교체하세요
  - 주의: 문서와 달리 주소 뒤에 키를 붙이는 방식(`/submit/<키>`)은 JSON 요청에서 500 이 납니다. `/submit` 으로 보내고 본문에 `access_key` 를 넣어야 합니다(`sendQuote`)
  - 무료 한도 월 250건. 도메인 제한 기능을 찾지 못했으므로 키가 공개된 상태에서 스팸이 오면 hCaptcha 추가를 검토할 것. 허니팟(`f-web`)은 이미 있음
- 카카오맵: 연결 완료. 실제 카카오 서버로 확인했고 백양대로 312(35.1543/129.0173)에 마커가 찍힘
  - 등록된 JavaScript SDK 도메인: `https://sketchmetal.co.kr`, `https://www.sketchmetal.co.kr`, `https://sketchmetal.vercel.app`, `http://localhost:8000`. 주소를 또 늘리면 여기에 추가해야 하고, 안 하면 `domain mismatched` 오류
- 사실 확인 완료(2026-09-22, 사용자 확인): 국립과학관 기관명, "15년 이상" 표기, 공공기관명 게시(벡스코·김해공항·영화의전당·누리마루·사상고·송수초), 시공 사진 속 작업자 얼굴, 비전 약속 문구, WHY ③ 제목 — 모두 현재 내용 그대로 문제없다고 확인받았습니다. 다시 여쭤보지 말 것
- 인사말의 서명은 **대표가 직접 쓴 자필 서명**입니다(2026-09-23 교체). 사용자가 준 사진에서 배경을 지워 투명 webp 로 만들었고(`assets/img/company/sign.webp`, 700×334 무손실), `IMG.sign` 으로 불러 `.sign-img`(custom.css, 폭 13.5rem·모바일 10.5rem) 크기로 씁니다. 전에 쓰던 나눔손글씨 붓 장식(`.sign`)은 지웠습니다
- 사례 상세: 해시태그 칩 → 큰 사진(좌우 원형 화살표, 01 / 06 카운터) → 썸네일 줄(선택 시 금색 테두리) → 시공처·분야·문의 버튼 순서. 갤러리 동작은 `#galStage`/`data-gstep`/`data-gi` 가 맡음
- 동영상도 같은 갤러리에서 사진과 섞어 보여 줍니다. `images.js` 의 `VID` 에 키를 넣고 `META` 목록에 끼워 넣으면 됨(`assets/video/`). `galMedia()` 가 키를 보고 `<img>` 또는 `<video>` 를 그리고, 넘길 때 재생 중인 동영상을 멈춥니다
  - `preload="metadata"` 라 눌러야 내려받습니다(안데르센공원 두 편 합쳐 18.4MB). 썸네일은 첫 프레임 + 재생 표시. 카운터는 동영상일 때 재생바와 겹치지 않게 위로 올라갑니다
  - 두 편 모두 **오디오 트랙을 파일에서 제거**했습니다(`ffmpeg -c:v copy -an`). 화면에서 음소거한 게 아니라 소리 자체가 없습니다
- 시공사례 목록에는 개수를 표시하지 않음 — 탭의 사례 수, 주요 시공처 칩의 사례 수, 카드의 "사진 N장" 을 모두 뺐습니다. 사례 상세의 `01 / 06` 갤러리 카운터만 남깁니다
- 사례별 해시태그는 `TAGS` 에 있음(사례마다 5개). 사진과 사례명에서 뽑은 것이고 없는 사실은 넣지 않았음
- 배너 사진 칸은 가로로 길어서 4:3 사진은 높이 일부만 보임. 배너에는 가로로 넓게 읽히는 사진을 골라야 함(시공사례 `c47_51_0` 벡스코 코킹, 회사소개 `c34_0`)
- 네이버·카카오 아이콘은 단순하게 다시 그린 것
- 개인정보처리방침: `#/privacy` (`pagePrivacy()`). 개인정보 보호법 제30조 항목 12개를 채웠습니다. **여기 적힌 내용은 실제 동작과 맞아야 하므로 폼·수탁사·보관 기간이 바뀌면 방침과 견적문의 동의 문구를 같이 고쳐야 합니다**
  - 수탁: Web3Forms(문의 전달), Vercel(호스팅). Web3Forms 는 인도 업체이고 AWS·Cloudflare·Hetzner 를 쓰므로 **국외 이전**에 해당하고, 스팸 차단을 위해 CleanTalk 에 IP·이메일이 전달됩니다. 이 사실을 5항에 적고 거부 방법(전화·이메일 문의)도 안내했습니다
  - 보관 기간은 **최대 3년**. 수탁사 기본 보관 기간과 맞춘 값이라 따로 지우지 않아도 방침과 실제가 어긋나지 않습니다. 기간을 줄이려면 Web3Forms 화면에서 직접 삭제하는 절차가 필요하므로 방침도 같이 고칠 것
  - 쿠키·방문자 분석 도구는 쓰지 않습니다. 넣게 되면 1항을 고쳐야 합니다
- 화면을 옮기면 항상 맨 위로 올라갑니다(`route()`). 예외는 사례 상세에서 목록으로 돌아올 때뿐이고 이때만 `scrollMemo` 로 보던 자리를 되살립니다. 새 내용이 그려지는 동안 브라우저가 스크롤을 되돌려 놓는 일이 있어서 `requestAnimationFrame` 으로 한 번 더 맞춥니다 — 이 두 번째 호출을 빼면 탭을 바꿔도 화면이 중간에 걸립니다
- 견적문의 입력 제한: 연락처는 `fmtTel()` 이 숫자만 받아 `010-0000-0000` 꼴로 다시 쓰고 최대 11자리(02 는 10자리)까지만 받습니다. 문의 내용은 `MSG_MAX`(1000자)이고 칸 위에 남은 글자 수를 보여 줍니다. 성함 20 / 회사 40 / 이메일 60 / 현장 위치 50 자
- 홈 맨 아래 노란 띠(약속합니다)는 왼쪽 위를 네이비 삼각형(`7vw`)이 덮기 때문에, 글자가 눈에 보이는 노란 면의 위아래 가운데에 오도록 `md:pt-28`+`md:pt-12`(안쪽) / `md:pb-24` 로 맞춰 두었습니다(1024~1600px 에서 위아래 여백 86 / 88px). 삼각형 높이나 글꼴을 바꾸면 다시 맞출 것
- 로고 마크(563×754)와 상호명 이미지는 간판 배너에서 잘라 낸 것이라 큰 화면에서 약간 부드러움. 벡터 원본이 있으면 교체하고 3D 윤곽(`LOGO_CONTOUR`)도 다시 뽑아야 함

## 작업할 때 알아 둘 것
- 새 Tailwind 클래스를 쓰면 `npm install` 후 `npm run css` 로 `assets/css/tailwind.css` 를 다시 만들어야 함
- `assets/fonts/` 는 사이트에 쓰인 글자만 담은 부분 글꼴. 글자를 새로 추가하면 구글 글꼴이 대신 채우지만(온라인일 때), 정확히 맞추려면 다시 만들 것: Google Fonts 저장소(github.com/google/fonts, ofl/notosanskr, ofl/notoserifkr)의 가변 TTF를 받아 `fonttools varLib.instancer <ttf> wght=300:700`(명조는 400:700)으로 굵기 범위를 줄인 뒤, `index.html`·`app.js`·`images.js`에 쓰인 글자로 `pyftsubset --flavor=woff2` 실행
- 사진은 webp, 긴 변 1000px 안팎. 사례 추가 절차는 README 참고. 동영상은 `assets/video/` 에 mp4 로 두고, 지금 올린 두 편은 1080×1920 원본 화질 그대로입니다(오디오만 제거). 더 가볍게 하려면 다시 인코딩할 것. ffmpeg 은 시스템에 없지만 `pip install imageio-ffmpeg` 로 받은 실행 파일을 쓸 수 있습니다(`python -c "import imageio_ffmpeg;print(imageio_ffmpeg.get_ffmpeg_exe())"`)
- 글꼴(눈누 기초고딕)은 `fonts.css` 의 jsDelivr 주소로 불러옵니다. 라이선스는 수정·재배포까지 허용(유료 판매만 금지)이라 서브셋을 만들어도 되지만 지금은 원본을 그대로 씁니다. 굵기가 400 하나뿐이라 `font-bold`(600) 자리는 브라우저 합성 볼드입니다. 원본: noonnu.cc/font_page/1496
- 파일명에 `con`, `prn`, `aux`, `nul`, `com1`~`com9`, `lpt1`~`lpt9` 를 단독으로 쓰지 말 것. Windows 예약 장치 이름이라 git 이 색인하지 못하고(`unable to index file`) Windows 에서 clone 도 안 됨. 건설업등록증 사진이 원래 `con.webp` 였고 이 때문에 `con-cert.webp` 로 바꿨음(`images.js` 경로만 수정, 키 `IMG.con`·`data-zoom="con"` 은 그대로)
- 로컬 확인은 `python3 -m http.server 8000` (파일을 더블클릭으로 열면 3D 로고 텍스처가 막힐 수 있음)
- 이전 대화에서 만든 claude.ai 미리보기 링크는 별개 복사본이라 이 저장소를 고쳐도 자동으로 바뀌지 않음
