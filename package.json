{
  "name": "sketchmetal",
  "version": "1.0.0",
  "private": true,
  "description": "스케치금속건설 홈페이지 (정적 사이트)",
  "scripts": {
    "css": "tailwindcss -c tailwind.config.js -i src/input.css -o assets/css/tailwind.css --minify",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
